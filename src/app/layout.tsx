import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "PlannerAI — AI Planning Agent",
  description:
    "Transform your ideas into actionable plans with our multi-agent AI system. Get comprehensive problem breakdowns, market insights, risk analysis, and execution plans.",
  keywords: ["AI", "Planning", "Agent", "Multi-Agent", "Report", "Strategy"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col font-[family-name:var(--font-inter)]">
        {children}
      </body>
    </html>
  );
}
